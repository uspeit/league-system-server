{
  "name": "league-server",
  "version": "1.0.0",
  "description": "League management system",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=dev && node src/js/index.js",
    "test": "export NODE_ENV=test && jest",
    "test:unit": "export NODE_ENV=test && jest --testPathPattern=src/tests/unit-tests",
    "test:integration": "export NODE_ENV=test && jest --testPathPattern=src/tests/integration",
    "test:acceptance": "export NODE_ENV=test && jest --testPathPattern=src/tests/acceptance"
  },
  "repository": {
    "type": "git",
    "url": "league-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "sequelize": "^6.6.2",
    "sqlite3": "^4.2.0"
  },
  "type": "module",
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.14.0",
    "jest": "^27.0.3"
  }
}
